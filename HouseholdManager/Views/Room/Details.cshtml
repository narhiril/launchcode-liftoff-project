@model HouseholdManager.Models.ViewModels.RoomDetailViewModel
@using HouseholdManager.Data.API

@{
    ViewData["Title"] = Model.Name + " Details";
    var req = new IconRequestor();
    string iconMouseOverText = await req.GetMouseoverTextForIcon(Model.Icon,(IEnumerable<Icon>)ViewBag.Icons);
}

<h1><span class="icon-font" title=@iconMouseOverText>@Model.Icon</span> @Model.Name</h1>
@await Html.PartialAsync("_CreationButtonsPartial")


<div> 
    <hr class="homepage-break">
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.DirtLevel)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.DirtLevel) / 10
        </dd>
        <dt class = "col-sm-8">
            @Html.DisplayNameFor(model => model.Icon)
        </dt>
        <dd class = "col-sm-10" title=@iconMouseOverText>
            <span>
                <label class="icon-font">@Html.DisplayFor(model => model.Icon)</label>
                <label class="icon-slug-display-small">@iconMouseOverText</label>
            </span>
        </dd>
    </dl>
    <table class="table-bordered table-responsive table-hover">
        <tr>
            <th>Missions In @Model.Name</th>
        
            <th>Point Value</th>
       
            <th>Due Date</th>
        </tr>
            @foreach (Mission item in Model.Missions)
            {
            <tr>
                <td>@item.Name</td>
                
                    <td>@item.Point</td>
                
                    <td>@item.DueDate</td>
            </tr>
            }
    </table>
</div>
<div class="form-group">
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-success form-button"> <i class="fa-solid fa-pen-to-square"></i> Edit</a>
    <a asp-action="Index" class="btn btn-secondary form-button"> <i class="fa-solid fa-arrow-left"></i> Back to List</a>
</div>

